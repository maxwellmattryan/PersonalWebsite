// Contains commands for running front end npm scripts
object FrontendCommands {
    val install: String = "npm install"
    val serve: String = "npm run start"
    val test: String = "npm run test"
    val build: String = "npm run build"
}
